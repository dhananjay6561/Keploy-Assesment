export default function Callout({ title, link, children }) {
  return (
    <div style={{
      background: "var(--callout-bg, #f6edff)",
      border: "1px solid var(--callout-border, #c3aafe)",
      color: "var(--text)",
      padding: "16px",
      borderRadius: "8px",
      margin: "24px 0"
    }}>
      <strong>{title}</strong>
      <p>{children}</p>
      <a href="https://keploy.io/docs/server/installation/" style={{ color: "var(--orange)", fontWeight: 600 }}>
        👉 Go to Installation Guide
      </a>
    </div>
  )
}

  Before running this sample, make sure Keploy is installed on your system.